/*

REVERS STRING

*/
const tekstas = 'abcdef';
let revers = '';

for (let i = tekstas.length-1; i >= 0; i--) { revers += tekstas[i];
    console.log(i,'->',tekstas[i]);
    
}
console.log(tekstas,'->',revers);







